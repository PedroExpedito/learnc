#include <stdio.h>
#include <string.h>

class Car{
  private:
    char name[100];
    char plate[10];
  public:
    void set_name(char *name) {
      strcpy(this->name, name);
    }

  protected:
};

int main(void) {
  Car car;


  return 0;
}
