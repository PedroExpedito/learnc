#include <iostream> // Mesmo que o stdio do C, não sei porque não usar a do C

using namespace std; // para não precisar fazer std:: estranho


int main(void) {
  cout << "Ola Mundo!" << endl;
  return 0;
}
